<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kkl.kklplus.b2b.joyoung.mapper.JoyoungOrderCancelledMapper" >
	<insert id="insert"  keyProperty="id" useGeneratedKeys="true">
		insert into joyoung_order_cancelled(
		b2b_order_id,
		order_no,
		cancel_man,
		cancel_date,
		cancel_remark,
		process_flag,
		process_time,
		process_comment,
		create_by,
		create_date,
		update_by,
		update_date,
		quarter
		)
		VALUES
		(
		#{b2bOrderId},
		#{orderNo},
		#{cancelMan},
		#{cancelDate},
		#{cancelRemark},
		#{processFlag},
		#{processTime},
		#{processComment},
		#{createById},
		#{createDt},
		#{updateById},
		#{updateDt},
		#{quarter}
		)

	</insert>

	<update id="updateProcessFlag" >
		UPDATE
		joyoung_order_cancelled
		SET
		process_flag = #{processFlag},
		process_time = process_time+1,
		process_comment = #{processComment},
		update_date = #{updateDt}
		WHERE
		id = #{id}
	</update>
</mapper>